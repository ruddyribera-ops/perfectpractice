{
  "name": "math-platform-e2e",
  "version": "1.0.0",
  "description": "Playwright smoke tests for production Railway deploy",
  "scripts": {
    "smoke": "playwright test",
    "smoke:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
